<%= form_with model: task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_area :details %>

  <% unless task.new_record? %>
    <%= f.check_box :completed %>
    <%= f.label :completed, "Completed" %>
  <% end %>

  <%= f.submit %>
<% end %>
